name: usecase
description: "Application use cases and gateway interfaces"
publish_to: "none"
version: 0.1.0
environment:
  sdk: ">=3.8.0 <4.0.0"
dependencies:
  model:
    path: ../model
dev_dependencies:
  lints: ^5.0.0
  test: ^1.29.0
